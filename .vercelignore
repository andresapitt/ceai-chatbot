# Vercel should deploy ONLY the Node serverless function in /api.
# Hide the Python/Flask app so Vercel does not detect and build it instead
# (that is what was wrongly answering /api/chat with the Flask handler).
app.py
chatbot.py
requirements.txt
templates/

# Local terminal bot, course materials, and the old backend folder are not
# needed on the serverless deployment.
agents/
backend/
context.md
knowledge.md
*.png
.claude/
